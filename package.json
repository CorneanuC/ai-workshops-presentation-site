{
  "name": "ai-workshops-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "npm run dev -- --host 0.0.0.0 --port 8080 --strictPort",
    "preview": "vite preview --host 0.0.0.0 --port 8080 --strictPort"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.1.0"
  }
}
